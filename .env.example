# Database Settings
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgrespassword
POSTGRES_SERVER=localhost
POSTGRES_PORT=5432
POSTGRES_DB=movie_crossword_db

# SQLite Migration Settings (Optional for dataset migration)
SQLITE_DB_PATH=/path/to/MovieCrossWord.db
SQLITE_TABLE_NAME=movie_master

# Embedding Model Choice
EMBEDDING_MODEL_NAME=BAAI/bge-m3
